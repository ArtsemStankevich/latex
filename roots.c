#include <stdio.h>
#include <math.h>

int main()
{
	int a, b,c, b1, x1, x2, x;
	b1 = b*b ;
	double D = b1 - 4*a*c;
	if (D > 0){
	x1 = (-b + sqrt(D))/(2 * a);
	x2 = (-b - sqrt(D))/(2 * a);
	
}
    if (D == 0 ){
	x =  -b/2*a;
	
}
	if ( D < 0){
	

}
}
